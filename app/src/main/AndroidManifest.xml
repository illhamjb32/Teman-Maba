<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.login_page">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <!--
           The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
           Google Maps Android API v2, but you must specify either coarse or fine
           location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/temanmaba"
        android:label="@string/app_name"
        android:roundIcon="@drawable/temanmaba"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".WelcomeSlideInfo"></activity>
        <activity android:name=".ActivityAbout" />
        <activity android:name=".EditContact" />
        <activity android:name=".ActivityTambahContact" />
        <activity android:name=".ActivityContact" />
        <activity android:name=".EditTugas" />
        <activity android:name=".ActivityTampilTugas" />
        <activity android:name=".ActivityTugas" />
        <activity android:name=".EditMatkul" />
        <activity android:name=".ActivityTambahMatkul" />
        <activity android:name=".MatkulActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".DetailProfile" />
        <activity android:name=".DashboardApp" />
        <activity android:name=".ForgetPassword3" />
        <activity android:name=".ForgetPassword2" />
        <activity android:name=".ForgetPassword1" />
        <activity android:name=".ChangePassword" />
        <activity android:name=".EditActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".WelcomeHome" />
        <activity android:name=".WelcomePurple" />
        <activity android:name=".WelcomeBlue" />
        <activity android:name=".WelcomeRed" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>